---
phase: 04-benchmarking-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bench.sh
  - bench/workloads/01-agg-dispatch.sql
  - bench/workloads/02-fit-predict.sql
  - bench/workloads/03-ffi-micro.sql
  - bench/workloads/01-agg-dispatch-1m.sql
  - bench/README.md
autonomous: true
requirements: [PERF-01, PERF-02]
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "A user runs one documented command (bash scripts/bench.sh) and gets reported timings for the three workloads (PERF-01)"
    - "The harness loads the LOCAL build extension by explicit path, not the autoloaded community version (RESEARCH pitfall 1)"
    - "Each run writes a diffable timestamped results file under bench/results/ capturing timings + query output (PERF-01)"
    - "A --full flag runs the 1M-group variant for official before/after numbers; default runs the scaled 10K-group variant (PERF-02)"
    - "bench/README.md documents what each workload covers, how to run, and notes optional CI perf tracking (PERF-02)"
  artifacts:
    - scripts/bench.sh
    - bench/workloads/01-agg-dispatch.sql
    - bench/workloads/02-fit-predict.sql
    - bench/workloads/03-ffi-micro.sql
    - bench/workloads/01-agg-dispatch-1m.sql
    - bench/README.md
  key_links:
    - "bench.sh -> build/release/duckdb via -unsigned + explicit LOAD of the local .duckdb_extension path"
    - "bench.sh stdout redirect (NOT DuckDB .output) -> bench/results/bench-<timestamp>.md"
---

<objective>
Build the Phase-4 benchmark harness — the TRACER slice for this phase. One documented command
(`bash scripts/bench.sh`) loads the locally built extension, runs three representative workloads
(aggregate dispatch, scalar fit/predict, FFI-marshalling micro-bench) with DuckDB `.timer on`, and
captures timings to a diffable results file under `bench/results/`. This is the end-to-end path
proven before any optimization work: it is the measurement foundation that Plan 03 uses to produce
before/after numbers.

Covers PERF-01 (repeatable harness reporting timings over the three named workloads) and PERF-02
(reproducible locally, documented, optional CI perf tracking noted).

Purpose: Establish the "before" measurement capability so all later optimization (Plan 02 FFI
refactor, Plan 03 hotspots) can be proven with numbers from a single, stable harness.
Output: `scripts/bench.sh`, four workload SQL files, `bench/README.md`, and a `bench/results/` dir
populated on first run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-benchmarking-performance/04-CONTEXT.md
@.planning/phases/04-benchmarking-performance/04-RESEARCH.md
@.planning/phases/04-benchmarking-performance/04-PATTERNS.md

# Analog files (read patterns, do not copy blindly):
@examples/performance_1m_groups/run_all_benchmarks.sh
@examples/performance_1m_groups/benchmark_ols.sql
@examples/performance_1m_groups/benchmark_ols_predict_agg.sql
@scripts/check_code_quality.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end benchmark harness — one command, three workloads, one results file</name>
  <files>scripts/bench.sh, bench/workloads/01-agg-dispatch.sql, bench/workloads/02-fit-predict.sql, bench/workloads/03-ffi-micro.sql, bench/workloads/01-agg-dispatch-1m.sql</files>
  <read_first>
    - scripts/check_code_quality.sh:1-15 — canonical header: `set -e`, `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`, `REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"`. bench.sh uses the stricter `set -euo pipefail` per 04-PATTERNS.md.
    - examples/performance_1m_groups/run_all_benchmarks.sh:18-47 — `run_benchmark()` shape and dispatch loop (drop the RSS-monitoring loop; not needed here).
    - examples/performance_1m_groups/benchmark_ols.sql (full) — W2 window-function shape, `.timer on` preamble, dataset CTE.
    - examples/performance_1m_groups/benchmark_ols_predict_agg.sql (full) — W1 aggregate shape.
    - 04-RESEARCH.md "Build & Extension Loading", "Benchmark Mechanics", "bench.sh Design Pattern", and Common Pitfalls 1-2 & 6.
    - 04-PATTERNS.md "scripts/bench.sh" and the three workload sections (verified dataset scale-down: `i % 10000` + `generate_series(1, 1000000)`).
  </read_first>
  <action>
    Create the harness that runs the three representative workloads end-to-end against the LOCAL release build and captures timings to a diffable file. This is the tracer: prove the full path (build binary -> explicit LOAD -> timed workload -> captured results file) works before any expansion.

    scripts/bench.sh:
    - Header: `#!/usr/bin/env bash`, `set -euo pipefail`, derive `SCRIPT_DIR` and `REPO_ROOT` per the check_code_quality.sh pattern (BASH_SOURCE form).
    - Resolve `DUCKDB="$REPO_ROOT/build/release/duckdb"` and `EXT="$REPO_ROOT/build/release/extension/anofox_statistics/anofox_statistics.duckdb_extension"`.
    - Fail fast with a clear message to stderr and exit 1 if `$DUCKDB` is not executable or `$EXT` does not exist — instruct the user to run `make release` (the precondition below).
    - Create `bench/results/` with `mkdir -p`; compute `TIMESTAMP=$(date +%Y%m%d-%H%M%S)`; `OUTFILE="$REPO_ROOT/bench/results/bench-$TIMESTAMP.md"`.
    - MUST load the local build by explicit path to avoid running the autoloaded community version (RESEARCH pitfall 1). Use `-unsigned` plus an explicit LOAD of `$EXT`. Prefer the `-cmd "LOAD '$EXT';"` flag (verified present in `duckdb --help` this session); if a runtime issue surfaces, fall back to `-init <(printf "LOAD '%s';\n" "$EXT")`. Do NOT rely on autoload; do NOT put a bare `LOAD 'anofox_statistics';` in the workload SQL.
    - `run_workload()` helper: takes a display name + sql file path; writes a `## <name>` markdown header and a fenced block to `$OUTFILE`; runs `"$DUCKDB" -unsigned -cmd "LOAD '$EXT';" -f "$sql_file" 2>&1 | tee -a "$OUTFILE"`. Capture the full process stdout+stderr (this is how `.timer` lines are collected — do NOT use DuckDB `.output`, per RESEARCH pitfall 2).
    - Default run executes W1 (01-agg-dispatch.sql), W2 (02-fit-predict.sql), W3 (03-ffi-micro.sql) at the scaled-down 10K-group / small-scale sizes.
    - `--full` flag additionally runs `01-agg-dispatch-1m.sql` (1M groups) for official before/after numbers. Parse the flag robustly (accept it in any argument position).
    - Print `Results written to: $OUTFILE` at the end.
    - Do NOT add community-extension autoload assumptions; do NOT add RSS monitoring; keep the script focused.

    Workload SQL files (each begins with `.timer on`; NONE contain a `LOAD` statement — the harness loads the extension):
    - bench/workloads/01-agg-dispatch.sql (W1, aggregate dispatch): dataset CTE scaled to `i % 10000` groups over `generate_series(1, 1000000)` with x1,x2,x3,y columns per the verified shape; run `anofox_stats_ols_fit_agg(y, [x1,x2,x3], {'intercept': true})` GROUP BY group_id; wrap in an outer `SELECT COUNT(*)` and materialize at least one struct field (e.g. `.r2`) so DuckDB cannot elide the aggregate. `.mode markdown`.
    - bench/workloads/02-fit-predict.sql (W2, scalar/window fit_predict): same scaled dataset; keep the exact window shape from benchmark_ols.sql — `anofox_stats_ols_fit_predict(y, [x1,x2,x3], {'fit_intercept': true}) OVER (PARTITION BY group_id ORDER BY row_num ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING)`; outer `SELECT COUNT(*) ... WHERE pred IS NOT NULL`. Confirm the exact option key (`fit_intercept` vs `intercept`) against test/sql before finalizing.
    - bench/workloads/03-ffi-micro.sql (W3, FFI marshalling micro-bench): small groups (e.g. `i % 500`) over `generate_series(1, 50000)` (~100 rows/group), running `anofox_stats_ols_fit_agg(y, [x1], {'intercept': true, 'compute_inference': true})` GROUP BY group_id — the `compute_inference: true` key forces the 5-array `libc::malloc` inference block on every group invocation (this is the exact block Plan 02 refactors). Materialize an inference field (e.g. `.std_errors[1]` or `.f_statistic`) in the outer query so the malloc path is not optimized away. Verify the exact inference field accessor against test/sql before finalizing.
    - bench/workloads/01-agg-dispatch-1m.sql (W1 full-scale): identical to 01-agg-dispatch.sql but `i % 1000000` over `generate_series(1, 100000000)` — the official 1M-group variant (documented ~8 GB RAM, ~160-210 s; local-only via --full).
  </action>
  <verify>
    <automated>bash scripts/bench.sh && ls bench/results/bench-*.md && grep -q "Run Time" "$(ls -t bench/results/bench-*.md | head -1)"</automated>
  </verify>
  <precondition>The release build exists: `build/release/duckdb` is executable and `build/release/extension/anofox_statistics/anofox_statistics.duckdb_extension` exists (run `make release` if not). [user_setup — prior build artifact]</precondition>
  <reversibility rating="reversible">New files only (scripts/bench.sh, bench/); no existing behavior touched. Trivially revertible.</reversibility>
  <done>`bash scripts/bench.sh` exits 0, writes a timestamped markdown file under bench/results/ containing per-statement `Run Time (s)` lines for all three default workloads; `--full` additionally runs the 1M-group variant; the workload SQL files contain no LOAD statement and the extension is loaded by the harness against the local build path.</done>
</task>

<task type="auto">
  <name>Task 2: Document the benchmark harness (bench/README.md) and note optional CI perf tracking</name>
  <files>bench/README.md</files>
  <read_first>
    - examples/performance_1m_groups/README.md — existing benchmark documentation tone and the resource/runtime numbers table to reference.
    - 04-RESEARCH.md "bench.sh Design Pattern" and Common Pitfall 6 (CI timeout / --full is local-only).
    - The scripts/bench.sh created in Task 1 — document its actual flags and output location.
  </read_first>
  <action>
    Write bench/README.md documenting the harness so runs reproduce locally (PERF-02).
    Include: (1) the single documented run command `bash scripts/bench.sh` and the `--full` variant; (2) a table describing each of the three workloads — W1 aggregate dispatch, W2 scalar fit/predict window, W3 FFI marshalling micro-bench — what path each exercises and its default scale (10K groups / small-scale) vs. the `--full` 1M-group scale; (3) where results are written (`bench/results/bench-<timestamp>.md`) and how to diff two runs (`diff` / `git diff` on the markdown, per RESEARCH "Don't Hand-Roll"); (4) the resource/runtime expectation for `--full` (~8 GB RAM, ~160-210 s, local-only) and why it is not in default/CI runs; (5) a short "CI perf tracking (optional)" section noting that wiring benchmarks into a gating CI job is intentionally NOT done this phase (per D-CONTEXT: PERF-02 requires it noted, not enforced) and sketching how it could be added later. Do NOT wire any CI job.
  </action>
  <verify>
    <automated>test -f bench/README.md && grep -qi "scripts/bench.sh" bench/README.md && grep -qi -- "--full" bench/README.md</automated>
  </verify>
  <reversibility rating="reversible">Documentation-only new file.</reversibility>
  <done>bench/README.md exists, documents the one run command, the three workloads and their scales, the results-file location and diffing, and includes an "optional CI perf tracking (not enforced this phase)" note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → duckdb CLI | bench.sh invokes the local duckdb binary with a fixed extension path; no external/user input crosses in |
| SQL → extension FFI | workload SQL calls the extension aggregate/window functions with generated (`random()`) data; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | bench.sh loads wrong extension (autoloaded community build vs local) | medium | mitigate | Explicit `-unsigned` + `LOAD '$EXT'` by absolute local path; no bare autoload LOAD in workloads (RESEARCH pitfall 1) |
| T-04-02 | Denial of Service | `--full` 1M-group run exhausts memory on constrained machines | low | accept | Documented ~8 GB RAM requirement; `--full` is opt-in and local-only, never default/CI |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan (pure bash + SQL + docs); no legitimacy gate needed |
</threat_model>

<verification>
- `bash scripts/bench.sh` exits 0 and writes `bench/results/bench-<timestamp>.md` containing `Run Time (s)` timing lines for W1, W2, W3.
- The results file shows the extension loaded from the local build path (no reliance on autoload).
- `bash scripts/bench.sh --full` additionally runs the 1M-group workload.
- bench/README.md documents the run command, workloads, results location, and the optional-CI note.
- No existing `test/sql` or `cargo test` behavior is touched (this plan adds only new files).
</verification>

<success_criteria>
- One documented command runs the three-workload suite and reports timings (PERF-01).
- Runs reproduce locally with documented scope and a `--full` official variant; optional CI perf tracking is noted, not enforced (PERF-02).
- Results are written to a diffable timestamped file under bench/results/.
</success_criteria>

<output>
Create `.planning/phases/04-benchmarking-performance/04-01-SUMMARY.md` when done.
</output>
