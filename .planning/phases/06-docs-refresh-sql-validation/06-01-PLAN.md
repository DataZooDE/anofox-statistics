---
phase: 06-docs-refresh-sql-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate_docs_sql.py
  - docs/API_CONVENTIONS.md
autonomous: true
requirements: [DOCS-02]
tags: [docs, validation, harness, tracer, duckdb]

estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Running `python3 scripts/validate_docs_sql.py` extracts every non-skipped sql code block from the 7 doc files, runs each file's blocks against the built extension, and prints a per-file PASS/FAIL report (DOCS-02)"
    - "The harness exits non-zero when any doc file's SQL fails and exits zero only when all executed files pass"
    - "A fenced block whose info-string carries the skip keyword is excluded from execution (skip-marker convention established)"
    - "The two migration 'Before' blocks in docs/API_CONVENTIONS.md are skip-marked so the harness does not execute them"
  artifacts:
    - "scripts/validate_docs_sql.py — the doc-SQL validation harness (the phase tracer)"
    - "docs/API_CONVENTIONS.md — two migration 'Before' blocks skip-marked"
  key_links:
    - "Harness -> build/release/duckdb -unsigned -cmd LOAD '<ext>' -f <tmp> (exit code is the pass/fail signal)"
    - "Skip-marker regex -> excludes skip-marked blocks before concatenation"
    - "--file flag -> single-file fast-iteration path consumed by Plans 02/03 verify commands"
---

<objective>
Build and self-test the doc-SQL validation harness `scripts/validate_docs_sql.py` (DOCS-02) — the measurement foundation for the whole phase. It extracts every fenced sql block from the 7 doc files, runs each file's blocks (concatenated in document order, honoring the skip marker) against the built extension via the DuckDB CLI, and exits non-zero on any failure with a per-file report. It SELF-TESTS (known-bad file exits non-zero, trivial-good file exits zero) before any fix work begins. As part of establishing the skip convention, mark the two intentional migration "Before" blocks in docs/API_CONVENTIONS.md as skip so they are excluded.

This is a TRACER: it wires one path (doc file -> extract -> LOAD ext -> run -> report -> exit code) end-to-end through every layer the phase touches, and it defines what "passing" means for Plans 02/03/04.

Purpose: Without a working, self-tested harness, "every example passes" (DOCS-03) and the CI gate (DOCS-04) have no measurement. The harness must exist and be proven correct FIRST.
Output: `scripts/validate_docs_sql.py`; two skip-marked blocks in `docs/API_CONVENTIONS.md`; a baseline failure count printed by the first full run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-docs-refresh-sql-validation/06-CONTEXT.md
@.planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md

# Prior-art the harness mirrors (DuckDB CLI invocation + repo-root pattern)
@scripts/bench.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Build scripts/validate_docs_sql.py end-to-end (extract, run, report, exit)</name>
  <files>scripts/validate_docs_sql.py</files>
  <read_first>
    - scripts/bench.sh (lines 17-24: SCRIPT_DIR/REPO_ROOT resolution, DUCKDB/EXT path vars; lines 44-53: precondition check; line 98: the exact `"$DUCKDB" -unsigned -cmd "LOAD '$EXT';" -f "$sql_file"` invocation this harness must mirror)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 2 (verified binary paths + LOAD incantation), section 5 (skip-marker + concrete Python approach)
  </read_first>
  <precondition>build/release/duckdb and build/release/extension/anofox_statistics/anofox_statistics.duckdb_extension exist (run `make release` if absent). The harness must hard-fail with a `make release` hint when they are missing — mirror bench.sh:44-53.</precondition>
  <action>
    Create a Python 3 script that resolves REPO from `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`, and DUCKDB / EXT to the two build/release paths named in RESEARCH section 2. On startup, if either path is missing, print an error naming the missing path plus a "build it first with: make release" hint and exit 1 (mirror bench.sh preconditions).

    Define the doc-file list exactly as RESEARCH section 5: README.md, guides/01_quick_start.md, guides/02_technical_guide.md, guides/03_business_guide.md, guides/04_advanced_use_cases.md, docs/API_REFERENCE.md, docs/API_CONVENTIONS.md.

    Extract fenced blocks with a MULTILINE+DOTALL regex that captures the info-string after the sql language tag and the block body, so a fence opened with the bare sql info-string is executed and one opened with the sql-plus-skip info-string is excluded. Track each block's starting line number (count newlines before the match) for the report.

    For each file: filter out skip-marked blocks, and if any executable blocks remain, concatenate them in document order into one temp .sql file (use tempfile; delete after). Prepend the DuckDB dot-command that makes the CLI stop and return non-zero at the first failing statement (the bail-on dot-command) as the first line of the temp file — VERIFIED this session: without it a good file exits 0 and a file whose first statement errors exits 1, and bail-on guarantees a mid-file error aborts the remaining concatenated blocks. Invoke `subprocess.run([DUCKDB, "-unsigned", "-cmd", f"LOAD '{EXT}';", "-f", tmpfile], capture_output=True, text=True)`. Treat `result.returncode != 0` as failure; record the file, the returncode, and result.stderr.

    Print one line per file: a PASS line with the relpath and executed-block count, or a FAIL line with the relpath followed by the captured stderr. At the end print a summary line with the total executed-file count and the failed-file count so the run produces a baseline failure number. Exit 1 if any file failed, else 0.

    Support a `--file <path>` argument that restricts the run to a single doc file for fast iteration (consumed by Plans 02/03). Validate the argument resolves to a path inside REPO and reject any path containing a parent-directory component (RESEARCH Security V5 / path-traversal mitigation, threat T-6-01); print an error and exit 2 on an out-of-tree path.

    Add a module docstring documenting the skip convention: fenced sql blocks carrying the skip keyword in their info-string are excluded from validation (illustrative/migration examples or examples needing external data). Keep the docstring about harness mechanics only — do not paste example SQL keywords or old prefixed function names into comments.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && python3 -c "import ast; ast.parse(open('scripts/validate_docs_sql.py').read()); print('parse-ok')"</automated>
    <fails_when>scripts/validate_docs_sql.py has a Python syntax error or does not exist — the ast.parse raises and the command exits non-zero instead of printing parse-ok.</fails_when>
  </verify>
  <done>scripts/validate_docs_sql.py exists, parses as valid Python 3, resolves DUCKDB/EXT/REPO paths, extracts sql blocks honoring the skip info-string, runs each file's concatenated blocks through the DuckDB CLI with bail-on, prints a per-file PASS/FAIL report plus a baseline failure count, supports --file, rejects out-of-tree paths, and exits non-zero when any file fails.</done>
</task>

<task type="auto">
  <name>Task 2: Self-test the harness and skip-mark the two API_CONVENTIONS migration blocks</name>
  <files>scripts/validate_docs_sql.py, docs/API_CONVENTIONS.md</files>
  <read_first>
    - docs/API_CONVENTIONS.md (lines 220-246: section 5 Breaking Changes — the two migration blocks; the two to skip are opened at the fences on lines 228 and 240, which contain the old prefixed and pre-rename names)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 1 "Intentional skip blocks" and Pitfall 4
  </read_first>
  <action>
    First edit docs/API_CONVENTIONS.md: change the opening fence of the two section-5 migration blocks (the fence at line 228 and the fence at line 240) so their info-string carries the skip keyword, so the harness excludes them. These blocks intentionally show the pre-v0.3.0 prefixed and pre-rename names and are correct migration documentation — they must be skipped, NOT rewritten. Leave the other four API_CONVENTIONS.md sql blocks (fences at lines 95, 103, 209, 252) as executable.

    Then self-test the harness two ways using throwaway files placed at an in-tree temp path (because --file requires an in-repo path): (a) a file containing one sql block that calls a function that does not exist in the extension — confirm the harness exits non-zero and prints FAIL; (b) a file containing one trivial sql block that must succeed — confirm the harness exits zero and prints PASS. Remove the throwaway files after; do not commit them.

    Finally run the harness against docs/API_CONVENTIONS.md via --file and confirm it exits zero (the four executable blocks pass, the two migration blocks are skipped). Record the three self-test exit codes in the plan SUMMARY as the tracer proof.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && printf '```sql\nSELECT anofox_stats_does_not_exist_fn(1);\n```\n' > .scratch_docs_selftest.md && python3 scripts/validate_docs_sql.py --file .scratch_docs_selftest.md; bad=$?; printf '```sql\nSELECT 1 AS ok;\n```\n' > .scratch_docs_selftest.md && python3 scripts/validate_docs_sql.py --file .scratch_docs_selftest.md; good=$?; rm -f .scratch_docs_selftest.md; python3 scripts/validate_docs_sql.py --file docs/API_CONVENTIONS.md; conv=$?; echo "bad=$bad good=$good conv=$conv"; test "$bad" -ne 0 && test "$good" -eq 0 && test "$conv" -eq 0</automated>
    <fails_when>The known-bad file exits 0 (harness misses failures), OR the trivial-good file exits non-zero (false positive), OR API_CONVENTIONS.md exits non-zero (the two migration blocks were not skipped). Any of these makes the final compound test exit non-zero.</fails_when>
  </verify>
  <done>The two API_CONVENTIONS.md migration blocks carry the skip info-string; the harness self-test proves known-bad exits non-zero, trivial-good exits zero, and API_CONVENTIONS.md exits zero. The tracer path (extract -> LOAD -> run -> report -> exit code) is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc file -> harness | The harness reads SQL text out of repo doc files and executes it against a local DuckDB. Doc content is version-controlled and PR-reviewed, but a PR could introduce a hostile block. |
| harness -> DuckDB CLI | Extracted SQL runs against a throwaway local database with no attached secrets or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-01 | Tampering | scripts/validate_docs_sql.py executing doc SQL | medium | mitigate | Harness runs each block against a throwaway in-process DuckDB (no ATTACH of persistent DBs, no secrets in env, CLI has no network by default); `--file` rejects any path containing a parent-directory component and any path resolving outside REPO, so it cannot be pointed at arbitrary files. Documented as an isolation note in the harness docstring. |
| T-6-01b | Tampering | path argument to `--file` | low | mitigate | Reject `..` components and out-of-tree resolved paths; exit 2 with an error. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('scripts/validate_docs_sql.py').read())"` succeeds (valid Python).
- Self-test triad: known-bad file exits non-zero, trivial-good exits zero, docs/API_CONVENTIONS.md exits zero.
- `python3 scripts/validate_docs_sql.py` (full sweep) runs to completion and prints a per-file report plus a baseline failure count (it is EXPECTED to exit non-zero at this point — the fix sweep is Plan 02; the baseline count is the deliverable here, not a green run).
</verification>

<success_criteria>
- scripts/validate_docs_sql.py exists, is valid Python 3, and mirrors bench.sh's path/precondition pattern.
- The skip-marker convention is established and documented in the harness docstring.
- The two API_CONVENTIONS.md migration blocks are skip-marked (not rewritten).
- The harness self-test proves it correctly signals pass (exit 0) and fail (exit non-zero).
- A baseline failure count is recorded in the SUMMARY (expected ~80 prefix + ~8 positional + ~4 stale-field blocks per RESEARCH).
</success_criteria>

<output>
Create `.planning/phases/06-docs-refresh-sql-validation/06-01-SUMMARY.md` when done, recording: the harness self-test exit codes, the skip-marker convention chosen, and the baseline full-sweep failure count (per-file pass/fail breakdown).
</output>