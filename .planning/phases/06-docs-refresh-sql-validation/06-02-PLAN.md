---
phase: 06-docs-refresh-sql-validation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - guides/01_quick_start.md
  - guides/02_technical_guide.md
  - guides/03_business_guide.md
  - guides/04_advanced_use_cases.md
  - docs/API_REFERENCE.md
autonomous: true
requirements: [DOCS-03]
tags: [docs, sql, migration, api-rename, duckdb]

estimate:
  tokens: 90000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every non-skipped sql block in guides/01-04 and docs/API_REFERENCE.md runs successfully against the built extension (DOCS-03)"
    - "No sql block calls a function under the removed `anofox_stats_` prefix, uses the removed `.r2` field, or uses `theilsen`"
    - "Old positional-boolean fit calls are converted to MAP-option style using only keys documented in docs/API_CONVENTIONS.md"
    - "Blocks that depend on external data not creatable inline are either made self-contained or skip-marked, and the harness passes for all five files"
  artifacts:
    - "guides/01_quick_start.md, guides/02_technical_guide.md, guides/03_business_guide.md, guides/04_advanced_use_cases.md — every sql block conforms to the v0.3.0 API"
    - "docs/API_REFERENCE.md — every sql block conforms to the v0.3.0 API"
  key_links:
    - "Fixed blocks -> docs/API_CONVENTIONS.md (authoritative names, option keys, return fields)"
    - "python3 scripts/validate_docs_sql.py --file <path> -> per-file green gate"
    - "Cross-block state -> CREATE TABLE / VALUES blocks precede SELECT blocks in each file (document order session)"
---

<objective>
Fix every failing SQL example across the four guides and docs/API_REFERENCE.md so the doc-SQL harness (Plan 01) exits zero for each file (DOCS-03). The dominant work is the `anofox_stats_` prefix sweep (80 blocks), plus converting old positional-boolean fit calls to MAP-option style (~8 blocks), correcting stale field names (`.f_statistic`, `.f_pvalue`, `.n_observations`, `full_output` — 4 blocks), and making external-data-dependent blocks self-contained or skip-marking them. Every fix conforms to docs/API_CONVENTIONS.md.

Purpose: DOCS-03 requires every documented example to pass against the final v0.3.0 API. The harness from Plan 01 defines "passing"; this plan drives its failure count to zero for these five files.
Output: five doc files whose non-skipped sql blocks all pass the harness.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md
@.planning/phases/06-docs-refresh-sql-validation/06-01-SUMMARY.md

# Authoritative naming / option-key / return-field reference every fix must conform to
@docs/API_CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix guides/01-02 (quick start + technical guide)</name>
  <files>guides/01_quick_start.md, guides/02_technical_guide.md</files>
  <read_first>
    - docs/API_CONVENTIONS.md (section 1 names, section 2 option keys, section 3 return fields — the authoritative targets)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 1 (violation table: guides/01 has 12 violation blocks incl. stale fields, guides/02 has 1 window block), Pitfalls 2/3/5
    - .planning/phases/06-docs-refresh-sql-validation/06-01-SUMMARY.md (baseline per-file failure list from the harness)
  </read_first>
  <action>
    Run `python3 scripts/validate_docs_sql.py --file guides/01_quick_start.md` and `--file guides/02_technical_guide.md` to see the concrete failures, then fix every failing block so both files pass.

    Apply these transformations, driven by the harness output (not by blanket find/replace): strip the removed prefix from every function call so it uses the unprefixed name; replace any removed `full_output`-style option and stale return-field references (the F-statistic and observation-count fields flagged in RESEARCH) with the correct v0.3.0 fields from API_CONVENTIONS.md section 3; convert any positional-boolean fit call to a MAP literal using only documented option keys (fit_intercept / compute_inference / confidence_level etc.); use `r_squared` never the removed `.r2`; use `theil_sen*` never `theilsen*`.

    For any block that fails because it selects from a table not created by an earlier block in the same file, make the block self-contained (inline the data via a VALUES/CTE or add a preceding CREATE TABLE block in document order) OR, if it is purely illustrative and cannot be made runnable in CI, mark its fence with the skip info-string established in Plan 01 and note why. Prefer making it runnable; skip only as a last resort.

    Do not change prose or narrative — only the sql blocks and any minimal surrounding text needed to keep an example coherent. Iterate until the harness exits zero for each file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && python3 scripts/validate_docs_sql.py --file guides/01_quick_start.md && python3 scripts/validate_docs_sql.py --file guides/02_technical_guide.md</automated>
    <fails_when>Any sql block in guides/01 or guides/02 errors against the built extension — the harness prints FAIL and exits non-zero.</fails_when>
  </verify>
  <done>python3 scripts/validate_docs_sql.py --file guides/01_quick_start.md and --file guides/02_technical_guide.md both exit zero; no removed prefix, removed field, or positional-boolean call remains executable in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix guides/03-04 (business + advanced use cases)</name>
  <files>guides/03_business_guide.md, guides/04_advanced_use_cases.md</files>
  <read_first>
    - docs/API_CONVENTIONS.md (authoritative names/keys/fields)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 1 (guides/03 = 15 violation blocks, guides/04 = 16 incl. an observation-count field block), Pitfalls 2 and 3 (positional-bool + external-table dependence are concentrated here)
    - .planning/phases/06-docs-refresh-sql-validation/06-01-SUMMARY.md (baseline failures for these two files)
  </read_first>
  <action>
    Run the harness with `--file guides/03_business_guide.md` and `--file guides/04_advanced_use_cases.md`, then fix every failing block with the same transformation set as Task 1 (unprefix, MAP-option conversion for positional booleans, correct return-field names, r_squared, theil_sen).

    These two guides concentrate the external-table dependence pitfall (blocks selecting FROM historical_sales, daily_prices, etc.). For each such block: if an earlier block in the same file already creates the table it can rely on the document-order session; otherwise add a self-contained CREATE TABLE ... AS VALUES (or CTE) block immediately before the first consumer, in document order, or skip-mark a genuinely non-runnable illustrative block with a one-line reason. Prefer runnable over skip.

    Iterate until the harness exits zero for both files. Keep prose untouched except where a data-setup sentence must be added to explain an inserted setup block.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && python3 scripts/validate_docs_sql.py --file guides/03_business_guide.md && python3 scripts/validate_docs_sql.py --file guides/04_advanced_use_cases.md</automated>
    <fails_when>Any sql block in guides/03 or guides/04 errors against the built extension — the harness prints FAIL and exits non-zero.</fails_when>
  </verify>
  <done>python3 scripts/validate_docs_sql.py --file guides/03_business_guide.md and --file guides/04_advanced_use_cases.md both exit zero; external-table-dependent examples are self-contained or explicitly skip-marked.</done>
</task>

<task type="auto">
  <name>Task 3: Fix docs/API_REFERENCE.md (157 blocks, 33 with violations)</name>
  <files>docs/API_REFERENCE.md</files>
  <read_first>
    - docs/API_CONVENTIONS.md (authoritative names/keys/fields — API_REFERENCE must agree with it exactly)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 1 (33 violation blocks of 157; Assumption A3 — most are prefix-only), Open Question 2 (many blocks reference FROM data / FROM tbl without inline setup)
    - .planning/phases/06-docs-refresh-sql-validation/06-01-SUMMARY.md (baseline API_REFERENCE failure list)
  </read_first>
  <action>
    Run `python3 scripts/validate_docs_sql.py --file docs/API_REFERENCE.md`, read the harness output, and fix every failing block. Most failures are the removed prefix; strip it. Apply the same v0.3.0 transformations as Tasks 1-2 to any positional-boolean / stale-field / theilsen / r2 occurrences the harness surfaces.

    Many API_REFERENCE blocks are signature illustrations that SELECT FROM a generic table (data, tbl) with no inline setup and cannot run standalone. For each failing block of that shape, prefer converting it to a self-contained form using literal arrays or a small inline VALUES table so it actually validates; only where a block is a pure signature sketch that cannot be made runnable, mark its fence with the skip info-string and add a one-line reason. Do not skip-mark a block merely because it uses the old prefix — those must be fixed, not skipped.

    Because API_REFERENCE is large, work in document order and re-run the harness after each cluster of edits so cross-block table state stays consistent. Finish when the harness exits zero for the file.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && python3 scripts/validate_docs_sql.py --file docs/API_REFERENCE.md</automated>
    <fails_when>Any non-skipped sql block in docs/API_REFERENCE.md errors against the built extension — the harness prints FAIL and exits non-zero.</fails_when>
  </verify>
  <done>python3 scripts/validate_docs_sql.py --file docs/API_REFERENCE.md exits zero; no removed prefix / removed field / positional-boolean call remains executable; skip-marks used only for pure signature sketches with a stated reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited doc SQL -> harness -> DuckDB | Fixed examples are executed against a throwaway local DuckDB; no new external surface is introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-01 | Tampering | doc SQL executed by the harness | medium | mitigate | Inherited from Plan 01: throwaway local DB, no secrets/network. This plan only edits SQL text; the isolation is unchanged. No new mitigation required. |
| T-6-03 | Information Disclosure | inline sample data added to make blocks self-contained | low | accept | Sample data is synthetic/illustrative; no real or sensitive data is introduced into docs. |
</threat_model>

<verification>
- Per-file: `python3 scripts/validate_docs_sql.py --file <each of the five files>` exits zero.
- Full sweep of these files aggregates to zero failures for guides/01-04 + docs/API_REFERENCE.md.
- Spot-grep confirms no executable block retains the removed prefix (skip-marked migration blocks in API_CONVENTIONS.md are out of scope for this plan).
</verification>

<success_criteria>
- All five files pass the harness (exit zero each).
- Positional-boolean fit calls converted to documented MAP options.
- Stale return fields replaced with API_CONVENTIONS.md section-3 fields.
- Skip-marks used sparingly and each carries a stated reason.
</success_criteria>

<output>
Create `.planning/phases/06-docs-refresh-sql-validation/06-02-SUMMARY.md` when done, recording: per-file before/after failure counts (from the Plan-01 baseline to zero), the number of blocks converted vs skip-marked, and any inline setup tables added.
</output>
