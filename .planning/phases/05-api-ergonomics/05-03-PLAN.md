---
phase: 05-api-ergonomics
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/window_functions/*.cpp
  - src/aggregate_functions/*.cpp
  - src/table_functions/*.cpp
  - src/scalar_functions/*.cpp
  - src/macros/fit_predict_macros.cpp
  - test/sql/*.test
  - test/sql/*.sql
  - docs/API_CONVENTIONS.md
  - test/sql/ergo03_naming.test
autonomous: false
requirements: [ERGO-03]

estimate:
  tokens: 85000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every SQL function is registered under one unprefixed name following {model}_{verb}[_agg|_predict] — the anofox_stats_ prefix is dropped everywhere and no alias blocks remain (ERGO-03)"
    - "theilsen is renamed to theil_sen everywhere (registration strings, macros, FunctionDescription examples, tests) (ERGO-03)"
    - "The documented convention exists in docs/API_CONVENTIONS.md covering function names, snake_case option keys, and the standard return-field set with the GLM z_values / AFT z_values per-family exception recorded (ERGO-03)"
    - "The full test suite (make test) and cargo test --workspace are green against the renamed API, including .r2 → .r_squared field references (Phase 5 success criterion 4)"
  artifacts:
    - "docs/API_CONVENTIONS.md — the written naming convention Phase 6 doc-SQL validation checks against"
    - "test/sql/ergo03_naming.test — smoke test that renamed names resolve and old prefixed names do not"
  key_links:
    - "Register*Function AggregateFunctionSet/ScalarFunctionSet primary string → single unprefixed name; alias block deleted; FunctionDescription.examples updated → DESCRIBE FUNCTION output matches"
    - ".r2 test references → .r_squared struct field exposed by GetXxxResultType builders"
---

<objective>
Do the clean, breaking cross-family rename (ERGO-03): drop the inconsistent `anofox_stats_` prefix so every function is registered under one unprefixed `{model}_{verb}[_agg|_predict]` name, fix `theilsen` → `theil_sen`, delete every short-alias registration block (the short name becomes the ONLY name), update `FunctionDescription.examples` strings, and update the entire `test/sql` suite — including the 50 files referencing the stale `.r2` field (correct field is `r_squared`) and the stale/typo function names in the guide tests. Write the convention down in a new `docs/API_CONVENTIONS.md` that Phase 6's doc-SQL validation will check against.

This is a locked, pre-approved ONE-WAY-DOOR decision (CONTEXT.md: "No deprecated aliases — clean rename", breaking renames allowed in early-dev). It breaks the published `anofox_stats_*` names and depends on Plans 01+02 so the renames land against already-validated behavior.

Purpose: deliver ERGO-03 (one documented naming convention consistent across all families) and Phase 5 success criterion 4 (test suite green against the new names).
Output: renamed registrations across ~87 cpp files, updated tests, `docs/API_CONVENTIONS.md`, `test/sql/ergo03_naming.test`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-ergonomics/05-CONTEXT.md
@.planning/phases/05-api-ergonomics/05-RESEARCH.md
@.planning/phases/05-api-ergonomics/05-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm the one-way-door cross-family rename with no aliases</name>
  <reversibility rating="one-way">The rename drops the published `anofox_stats_*` names and the `.r2` field with NO deprecated aliases — any external SQL, notebook, or downstream query referencing the old names or `.r2` breaks immediately and cannot be recovered without re-adding aliases. Pre-approved in CONTEXT.md (early-dev, breaking renames allowed), but recorded as a decision gate before the irreversible edit.</reversibility>
  <decision>Proceed with the clean, breaking cross-family rename (drop `anofox_stats_` prefix, delete all short-alias registration blocks, `theilsen` → `theil_sen`, rewrite the entire test/sql suite including 50 `.r2` files) with NO deprecated aliases retained.</decision>
  <context>This is a one-way door: the published `anofox_stats_*` names and the `.r2` field disappear with no fallback. It is a locked, pre-approved decision in 05-CONTEXT.md (early-dev, breaking renames allowed, "No deprecated aliases — clean rename"). This checkpoint exists to record the irreversible gate before the edit lands, not to relitigate the decision. `gate="blocking-human"` so an unattended auto-run still surfaces it to a human.</context>
  <options>
    <option id="proceed">
      <name>Proceed with the clean rename (recommended — matches locked CONTEXT.md decision)</name>
      <pros>Delivers ERGO-03's single consistent convention; no alias cruft; Phase 6 docs validate against the final API</pros>
      <cons>Irreversible break of the old `anofox_stats_*` names and `.r2` field for any external consumer</cons>
    </option>
    <option id="halt">
      <name>Halt and reconsider (e.g. keep temporary aliases)</name>
      <pros>Preserves backward compatibility for existing external queries</pros>
      <cons>Contradicts the locked CONTEXT.md decision; leaves the inconsistent naming ERGO-03 exists to remove</cons>
    </option>
  </options>
  <resume-signal>Type "proceed" to run the rename, or "halt" to stop and reconsider.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Rename all registrations — drop prefix, delete alias blocks, fix theilsen, update examples</name>
  <files>src/window_functions/*.cpp, src/aggregate_functions/*.cpp, src/table_functions/*.cpp, src/scalar_functions/*.cpp, src/macros/fit_predict_macros.cpp</files>
  <read_first>
    - src/aggregate_functions/ols_aggregate.cpp lines 377-425 (the full registration block: primary string + AddFunction strings + FunctionDescription.examples + the alias block to delete)
    - src/window_functions/ols_fit_predict.cpp lines 360-410 (window registration + alias block)
    - .planning/phases/05-api-ergonomics/05-RESEARCH.md "Naming Inventory and Rename Map" (the complete current→target table for every family)
    - .planning/phases/05-api-ergonomics/05-PATTERNS.md "Work Item 4" (before/after registration-collapse template + theil_sen two-part rule + examples-grep)
  </read_first>
  <action>
    Across `src/window_functions/`, `src/aggregate_functions/`, `src/table_functions/`, `src/scalar_functions/` and `src/macros/fit_predict_macros.cpp`, apply the registration collapse from PATTERNS.md Work Item 4 for every function in the RESEARCH.md rename map:
    1. Change the primary registration string (`AggregateFunctionSet("anofox_stats_X")` / `ScalarFunctionSet` / `AggregateFunction("anofox_stats_X", ...)` / table-function name) to the target unprefixed name.
    2. Delete the entire short-alias registration block (the secondary `*Set alias_set(...)` + `alias_info.alias_of = "anofox_stats_..."` + `RegisterFunction(alias_info)`), since the short name is now primary.
    3. Update `FunctionDescription.examples` strings in the same block to the new name (grep `grep -rn '"anofox_stats_' src/ | grep -i example`).
    4. Two-part `theilsen` → `theil_sen`: strip the prefix AND insert the underscore (registration strings, macro entry `theilsen_fit_predict_by` → `theil_sen_fit_predict_by`, and any describe/example strings).

    After the pass, no `"anofox_stats_` string literal should remain in `src/`. Use grep-driven verification, not blind sed — several "(check file)" entries in the rename map need confirming against the actual registration string in each file. Do not change function BEHAVIOR, signatures, state, or result-struct field names (except that this is where you confirm the GLM/AFT `z_values` exception stays and the linear family already exposes `r_squared` — no field rename needed per RESEARCH.md).
  </action>
  <acceptance_criteria>
    - source: `grep -rn '"anofox_stats_' src/` returns no matches (prefix fully dropped from every registration + example string)
    - source: `grep -rn 'alias_of = "anofox_stats_' src/` returns no matches (all alias blocks deleted)
    - source: `grep -rn '"anofox_stats_theilsen\|"theilsen_\|theilsen_fit_predict_by' src/` returns no matches; `grep -rn 'theil_sen' src/macros/fit_predict_macros.cpp` returns the renamed macro
    - behavior: the extension builds and loads (`make release` succeeds and the loadable extension registers without conflict)
  </acceptance_criteria>
  <verify>
    <automated>make release; echo "build_exit=$?"; grep -rn '"anofox_stats_' src/ | grep -v '^\s*//' | wc -l</automated>
    <fails_when>Build fails (a deleted alias block left a dangling brace, or a renamed function collides), OR the grep count of remaining `"anofox_stats_` literals in `src/` is greater than 0, OR an alias block was left registering `alias_of = "anofox_stats_..."` (runtime alias-lookup failure per Pitfall 2).</fails_when>
  </verify>
  <done>All registrations use unprefixed names, all alias blocks are deleted, theilsen is theil_sen everywhere in src/, examples updated, and the extension builds and loads.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the test/sql suite to the renamed API and get make test + cargo green</name>
  <files>test/sql/*.test, test/sql/*.sql, test/sql/ergo03_naming.test</files>
  <read_first>
    - .planning/phases/05-api-ergonomics/05-RESEARCH.md "Test File .r2 References" + "Stale Function References in Test Files" (the stale-name → correct-target table)
    - .planning/phases/05-api-ergonomics/05-PATTERNS.md Pitfall 4 (theilsen two-step sed) + Pitfall 3 (intercept alias must survive) + Pitfall 5 (examples)
    - test/sql/anofox_stats.test (a representative test using the prefixed names + option maps)
  </read_first>
  <action>
    Update every `test/sql/*.test` and `test/sql/*.sql` file to the renamed API:
    1. Strip the `anofox_stats_` prefix from all function references. Two-step for theilsen: `s/anofox_stats_theilsen/theil_sen/g` then `s/\btheilsen\b/theil_sen/g` (Pitfall 4) so no bare `theilsen` survives.
    2. Replace stale field `.r2` → `.r_squared` across the 50 files that use it. Verify `.residual_standard_error` vs `residual_std_error` in `guide01_pattern_4_full_statistical_workflow.sql` and fix to the actual exposed field name.
    3. Fix the stale/typo function names from the RESEARCH.md table: `anofox_stats_predict_ols` → `predict`, `anofox_stats_residual_diagnostics` → `residuals_diagnostics`, `anofox_stats_normality_test` → the real function (verify against a currently-registered name — likely `shapiro_wilk_agg`), and the `anofox_stats_expanding_ols` / `anofox_stats_rolling_ols` references → `ols_fit_predict_agg` with the appropriate `ROWS BETWEEN` frame.
    4. Do NOT touch the `{'intercept': ...}` option key — it is a valid accepted alias (Pitfall 3); only function NAMES and the `.r2` FIELD change.

    Create `test/sql/ergo03_naming.test`: a smoke test that (a) a sample of renamed names across families resolve (`ols_fit_agg`, `theil_sen_fit`, `poisson_fit_agg`, `t_test_agg`, `vif`) via a trivial call or `SELECT ... FROM duckdb_functions()` existence check, and (b) at least one old prefixed name (`anofox_stats_ols_fit_agg`) does NOT resolve (a `statement error` block asserting the function is not found).
  </action>
  <acceptance_criteria>
    - source: `grep -rn 'anofox_stats_' test/sql/` returns no matches (prefix removed from all tests)
    - source: `grep -rn '\.r2\b' test/sql/` returns no matches (all field references updated to `.r_squared`)
    - source: `grep -rn '\btheilsen\b' test/sql/` returns no matches (all fixed to theil_sen)
    - source: `test/sql/ergo03_naming.test` exists and asserts both that renamed names resolve and that a prefixed old name does not
    - behavior: `make test` full native SQL suite passes
  </acceptance_criteria>
  <verify>
    <automated>make test; echo "exit=$?"</automated>
    <fails_when>Any SQL test fails — a missed prefix, a leftover `.r2`, a bare `theilsen`, an unfixed stale function name, or `ergo03_naming.test` failing because a renamed name did not resolve or an old prefixed name still resolved.</fails_when>
  </verify>
  <done>The entire test/sql suite references only the renamed unprefixed API with `r_squared` fields, stale names are fixed, `ergo03_naming.test` proves the rename, and `make test` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/API_CONVENTIONS.md and confirm cargo workspace green</name>
  <files>docs/API_CONVENTIONS.md</files>
  <read_first>
    - .planning/phases/05-api-ergonomics/05-CONTEXT.md "Naming Convention (ERGO-03)" (the locked convention: name pattern, snake_case option keys, standard return-field set, GLM z_values exception)
    - .planning/phases/05-api-ergonomics/05-RESEARCH.md "Return Struct Field Inconsistencies" (the standard field set + the GLM/AFT z_values documented exceptions + ALM's different core fields)
  </read_first>
  <action>
    Write `docs/API_CONVENTIONS.md` documenting the shipped convention so Phase 6's doc-SQL validation can check examples against it:
    - Function naming: `{model}_{verb}[_agg|_predict]`, unprefixed and uniform; list the verb/suffix meanings (`_fit`, `_fit_predict`, `_agg`, `_predict`, scalar diagnostics); note the `anofox_stats_` prefix was dropped in v0.3.0 (breaking).
    - Option-map keys: `snake_case` matching the Rust core; list the common keys and documented accepted aliases (`intercept` for `fit_intercept`, etc.).
    - Return-struct fields: the standard set (`coefficients`, `intercept`, `std_errors`, `t_values`, `p_values`, `r_squared`, plus the linear-family extras) and the DOCUMENTED per-family exceptions: GLM family uses `z_values`, AFT uses `z_values` (survival convention), ALM omits `r_squared` and carries `log_likelihood`/`aic`/`bic`/`scale`. State explicitly these exceptions are intentional (do NOT force z→t).
    - A short "breaking changes in v0.3.0" note listing the dropped prefix, `theilsen`→`theil_sen`, and no deprecated aliases.

    Then run the cargo workspace tests to confirm the Rust side stays green (no Rust rename was needed, but the workspace must be verified green for Phase 5 success criterion 4).
  </action>
  <acceptance_criteria>
    - source: `docs/API_CONVENTIONS.md` exists and documents (a) the function-name pattern, (b) snake_case option keys with accepted aliases, (c) the standard return-field set AND the GLM/AFT `z_values` + ALM exceptions
    - source: the doc records the v0.3.0 breaking changes (dropped prefix, theilsen→theil_sen, no aliases)
    - command: `cargo test --workspace` passes (exit 0)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/API_CONVENTIONS.md && grep -qi 'z_values' docs/API_CONVENTIONS.md && grep -qiE 'theil_sen|no deprecated alias|breaking' docs/API_CONVENTIONS.md && cargo test --workspace; echo "exit=$?"</automated>
    <fails_when>`docs/API_CONVENTIONS.md` is missing, or omits the `z_values` per-family exception, or omits the breaking-change note, or `cargo test --workspace` fails.</fails_when>
  </verify>
  <done>`docs/API_CONVENTIONS.md` documents the full convention including the z_values exception and breaking changes; `cargo test --workspace` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL query → function-name resolution | User-supplied function names resolve against the renamed registry; stale names must fail cleanly (function-not-found), not silently mis-resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-03 | Repudiation / Confusion | Registration alias blocks | low | mitigate | Deleting alias blocks and collapsing to one canonical name removes the alias-lookup-failure ambiguity (Pitfall 2) — a call either resolves to the one true name or fails clearly; `ergo03_naming.test` proves old prefixed names no longer resolve (Task 2) |
| T-5-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase (RESEARCH.md §Package Legitimacy Audit: Not applicable) |
</threat_model>

<verification>
- `grep -rn '"anofox_stats_' src/` and `grep -rn 'anofox_stats_' test/sql/` both return no matches.
- `grep -rn '\.r2\b' test/sql/` and `grep -rn '\btheilsen\b' src/ test/sql/` return no matches.
- `docs/API_CONVENTIONS.md` exists and documents names, option keys, return fields, and exceptions.
- `make test && cargo test --workspace` both green against the renamed API.
</verification>

<success_criteria>
Every function follows one documented unprefixed convention with no aliases; theilsen→theil_sen fixed; the convention is written in `docs/API_CONVENTIONS.md`; the full native + cargo suites are green against the renamed API. Maps to ERGO-03 and ROADMAP Phase 5 success criteria 3 and 4.
</success_criteria>

<output>
Create `.planning/phases/05-api-ergonomics/05-03-SUMMARY.md` when done, listing the families renamed, the count of test files updated, and the resolved values for the previously-ambiguous "(check file)" and stale-name entries.
</output>
