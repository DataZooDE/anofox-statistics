---
phase: 06-docs-refresh-sql-validation
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01]
tags: [docs, readme, restructure]

estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "README.md follows the anofox-forecast form: emoji section headers, a Table of Contents, Key Features with a Performance subsection and a User-Friendly API subsection, a Quick Start walkthrough on a concrete dataset, an API Reference section linking to docs/, Development, Support, Citation, with License last (DOCS-01)"
    - "The README Quick Start uses only v0.3.0 API names/options and passes the doc-SQL harness"
    - "The README API Reference section links to docs/API_REFERENCE.md and docs/API_CONVENTIONS.md rather than duplicating the full surface"
  artifacts:
    - "README.md — restructured to the anofox-forecast form with all required sections in the locked order"
  key_links:
    - "README Quick Start sql -> python3 scripts/validate_docs_sql.py --file README.md (green gate)"
    - "Key Features Performance subsection -> Phase-4 benchmark work; User-Friendly API subsection -> Phase-5 ergonomics"
    - "API Reference section -> docs/API_REFERENCE.md + docs/API_CONVENTIONS.md links"
---

<objective>
Restructure README.md into the anofox-forecast form (DOCS-01): emoji section headers, a Table of Contents, a Key Features section with a Performance subsection (Phase-4 benchmark work) and a User-Friendly API subsection (Phase-5 ergonomics), a Quick Start walkthrough on one concrete dataset end-to-end (fit -> predict -> diagnostics) using the new v0.3.0 API, an API Reference section that links to docs/API_REFERENCE.md and docs/API_CONVENTIONS.md, Development, Support, Citation, and License moved to the end. The rewritten Quick Start must pass the doc-SQL harness.

Purpose: DOCS-01 requires the README to match the anofox-forecast form with correct, validated examples. The current Quick Start uses the removed prefix, the removed full_output option, and the removed .r2 field, so it must be rewritten, not patched.
Output: a restructured README.md whose Quick Start passes `python3 scripts/validate_docs_sql.py --file README.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-docs-refresh-sql-validation/06-CONTEXT.md
@.planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md

# Authoritative API names/options/fields the Quick Start must conform to
@docs/API_CONVENTIONS.md
# Current README to restructure
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README sections into anofox-forecast form</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/06-docs-refresh-sql-validation/06-CONTEXT.md (README Structure locked decisions — exact section order)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 4 (current-vs-target section table, the 9-item restructure list)
    - README.md (current content: badges lines 5-7, Features table lines 13-92, Installation 185-207, Support 301, Citation 319, License 254)
  </read_first>
  <action>
    Rewrite README.md into this exact section order (locked in CONTEXT.md), adding an emoji to every level-2 heading: Title + badges (keep existing badges block) -> Table of Contents (new; anchor links to each level-2 section) -> Key Features -> Quick Start -> Installation -> API Reference -> Development -> Support -> Citation -> License (last).

    Key Features: keep the existing regression/tests/diagnostics/macros function tables as the feature surface, and add two subsections: a Performance subsection that references the Phase-4 benchmark harness and results, and a User-Friendly API subsection that references the Phase-5 ergonomics work (clear/early error messages, consistent unprefixed naming). Do not fabricate specific benchmark numbers — describe the work and point to the bench artifacts; use concrete numbers only if present in the Phase-4 summary.

    API Reference: replace the current Documentation section with an API Reference section that links to docs/API_REFERENCE.md (authoritative full surface) and docs/API_CONVENTIONS.md (naming/option/return conventions) instead of duplicating the function list.

    Development: add a section covering building from source (make release), running the SQL test suite (build/release/test/unittest --test-dir=test/sql) and cargo test — fold in any still-relevant content from the current Dependencies section.

    Move License to the very end (after Citation). Merge or drop the current Validation, Acknowledgments, and Dependencies sections per RESEARCH section 4 items 7-8 (keep any live content by folding it into Development or Support; do not lose the anofox-regression / R-validation note — fold it into Development or a short note under Key Features).

    Do NOT touch the Quick Start sql yet (Task 2 rewrites it); leave a clearly-marked placeholder if needed so this task is a pure structural pass. Update all Table-of-Contents anchors to match the final headings.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && grep -q '^## .*Table of Contents' README.md && grep -q '^## .*Key Features' README.md && grep -q '^## .*Quick Start' README.md && grep -q '^## .*API Reference' README.md && grep -q '^## .*Development' README.md && grep -q '^## .*Citation' README.md && awk '/^## /{last=$0} END{print last}' README.md | grep -qi 'License'</automated>
    <fails_when>A required section heading (Table of Contents, Key Features, Quick Start, API Reference, Development, Citation) is missing, OR License is not the last level-2 section — one of the greps fails and the command exits non-zero.</fails_when>
  </verify>
  <done>README.md has all required level-2 sections in the locked anofox-forecast order with emoji headers; License is the final section; API Reference links to docs/API_REFERENCE.md and docs/API_CONVENTIONS.md; Key Features has Performance and User-Friendly API subsections.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Quick Start example so it passes the harness</name>
  <files>README.md</files>
  <read_first>
    - docs/API_CONVENTIONS.md (section 1 names, section 2 option keys, section 3 return fields — e.g. r_squared, coefficients, intercept)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md Pitfall 5 (the current Quick Start uses full_output + .r2 and must be rewritten from scratch)
  </read_first>
  <action>
    Replace the Quick Start sql with one concrete small dataset walked end-to-end using the v0.3.0 API: create or inline a small dataset, fit a model (unprefixed name, MAP options with documented keys), access documented return fields (coefficients, intercept, r_squared — never the removed full_output/.r2), then show a predict step and a diagnostics read. Also fix the "Naming Convention" prose block that currently claims an `anofox_stats_*` primary with unprefixed aliases — v0.3.0 has no prefix and no aliases; state the unprefixed convention and point to docs/API_CONVENTIONS.md.

    Make every sql block self-contained (literal arrays or an inline VALUES table) so it runs under the harness in document order. Remove the stale "all functions use positional parameters / named parameters" caveat if it no longer matches the MAP-option reality; keep only accurate guidance.

    Iterate against `python3 scripts/validate_docs_sql.py --file README.md` until it exits zero.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && python3 scripts/validate_docs_sql.py --file README.md</automated>
    <fails_when>Any non-skipped sql block in README.md errors against the built extension — the harness prints FAIL and exits non-zero.</fails_when>
  </verify>
  <done>python3 scripts/validate_docs_sql.py --file README.md exits zero; the Quick Start walks fit -> predict -> diagnostics on a concrete dataset with v0.3.0 names/options/fields and no removed prefix, full_output, or .r2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README Quick Start sql -> harness -> DuckDB | The Quick Start block is executed by the harness against a throwaway local DuckDB; no new external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-01 | Tampering | README Quick Start SQL executed by harness | low | mitigate | Inherited from Plan 01: throwaway local DB, no secrets/network. This plan only authors README SQL; isolation unchanged. |
</threat_model>

<verification>
- `python3 scripts/validate_docs_sql.py --file README.md` exits zero.
- Section-order grep gate (Task 1 automated verify) passes.
- Manual (verification phase): README renders as anofox-forecast form — emoji headers, ToC links resolve, Key Features has both subsections, License last.
</verification>

<success_criteria>
- README.md is in the locked anofox-forecast section order with emoji headers and a ToC.
- Key Features carries a Performance subsection and a User-Friendly API subsection.
- Quick Start passes the harness on a concrete end-to-end walkthrough.
- API Reference links to docs/ instead of duplicating the surface; License is last.
</success_criteria>

<output>
Create `.planning/phases/06-docs-refresh-sql-validation/06-03-SUMMARY.md` when done, recording: the final section list in order, the Quick Start dataset/model used, and the harness exit code for README.md.
</output>
