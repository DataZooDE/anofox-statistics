---
phase: 06-docs-refresh-sql-validation
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - .github/workflows/DocsSqlValidation.yml
autonomous: true
requirements: [DOCS-04]
tags: [ci, github-actions, docs, validation]

estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A .github/workflows/DocsSqlValidation.yml workflow builds the extension and runs python3 scripts/validate_docs_sql.py, hard-failing the job on any doc-SQL failure (DOCS-04)"
    - "The workflow triggers on pull_request and on push to main"
    - "With all docs already fixed (Plans 02/03), the harness step exits zero so the job is green on the current tree"
  artifacts:
    - ".github/workflows/DocsSqlValidation.yml — self-contained ubuntu-24.04 build-then-validate CI job"
  key_links:
    - "make release -> build/release/duckdb + extension -> harness"
    - "harness non-zero exit -> job failure (no continue-on-error) -> drift fails the build"
    - "triggers -> pull_request + push:branches:[main]"
---

<objective>
Add `.github/workflows/DocsSqlValidation.yml` (DOCS-04): a self-contained ubuntu-24.04 job that checks out with recursive submodules, installs the Rust toolchain, builds the extension with `make release`, then runs `python3 scripts/validate_docs_sql.py` — hard-failing the job (no continue-on-error) on any doc-SQL failure. It triggers on pull_request and on push to main, so any future documentation drift fails the build.

Purpose: DOCS-04 closes the loop — the harness only prevents drift if CI runs it as a gate. This mirrors the build-and-test-rust job's proven ubuntu-24.04 + dtolnay/rust-toolchain@stable + actions/cache pattern.
Output: a committed, syntactically valid workflow that gates doc-SQL drift.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md

# CI prior-art to mirror: the self-contained ubuntu-24.04 rust build job + trigger shape
@.github/workflows/MainDistributionPipeline.yml
@.github/workflows/WasmTest.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/DocsSqlValidation.yml</name>
  <files>.github/workflows/DocsSqlValidation.yml</files>
  <read_first>
    - .github/workflows/MainDistributionPipeline.yml (lines 141-181: the build-and-test-rust job — checkout submodules:recursive, dtolnay/rust-toolchain@stable, actions/cache@v4 with the ~/.cargo + target key; env CC=gcc CXX=g++)
    - .planning/phases/06-docs-refresh-sql-validation/06-RESEARCH.md section 3 (DocsSqlValidation.yml design Option B — the recommended self-contained build job, with the reusable steps yaml)
  </read_first>
  <action>
    Create a single-job workflow named for docs-SQL validation, triggered `on: pull_request` and `on: push` with `branches: [main]`, with `permissions: contents: read`. One job on `runs-on: ubuntu-24.04`, `env: { CC: gcc, CXX: g++ }`, mirroring the build-and-test-rust steps:
    - actions/checkout@v4 with `submodules: 'recursive'`.
    - dtolnay/rust-toolchain@stable.
    - actions/cache@v4 caching ~/.cargo/registry, ~/.cargo/git, and target with the same `${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}` key used by build-and-test-rust.
    - A build step running `make release` (produces build/release/duckdb + the extension).
    - A final step running `python3 scripts/validate_docs_sql.py` with NO continue-on-error, so a non-zero harness exit fails the job.

    Do not add matrix archs, deploy, or Windows legs — this is a lighter linux-only gate, per RESEARCH Option B. Keep the trigger and gating shape aligned with the repo's existing workflows.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && f=.github/workflows/DocsSqlValidation.yml && grep -vE '^\s*#' "$f" | grep -q 'make release' && grep -vE '^\s*#' "$f" | grep -q 'validate_docs_sql.py' && grep -vE '^\s*#' "$f" | grep -q 'pull_request' && grep -vE '^\s*#' "$f" | grep -q 'ubuntu-24.04' && grep -vE '^\s*#' "$f" | grep -qE 'branches:\s*\[?\s*main' && echo workflow-ok</automated>
    <fails_when>DocsSqlValidation.yml is missing the make-release build step, the harness run step, the pull_request trigger, the ubuntu-24.04 runner, or the push-to-main branch filter — the corresponding grep fails and the command exits non-zero without printing workflow-ok.</fails_when>
  </verify>
  <done>.github/workflows/DocsSqlValidation.yml exists as a single ubuntu-24.04 job that builds via make release and runs the harness with no continue-on-error, triggered on pull_request and push to main.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the harness passes locally as the CI would (full sweep green)</name>
  <files>.github/workflows/DocsSqlValidation.yml</files>
  <read_first>
    - .planning/phases/06-docs-refresh-sql-validation/06-02-SUMMARY.md and 06-03-SUMMARY.md (confirm Plans 02/03 drove per-file failures to zero)
  </read_first>
  <precondition>Plans 02 and 03 are complete (all guides, docs/API_REFERENCE.md, and README.md pass the harness); build/release contains a current extension (run `make release` if stale).</precondition>
  <action>
    Run the full doc-SQL sweep exactly as the new CI job will (`python3 scripts/validate_docs_sql.py` with no --file), and confirm it exits zero across all 7 files — this proves the CI gate will be green on the current tree rather than red on landing. If any file still fails, the fix belongs to Plan 02/03; record the failure and do not weaken the gate (do not add continue-on-error or skip-mark a real failure to make CI pass). Also confirm the pre-existing SQL regression suite still passes so the docs work introduced no regression.

    Record in the SUMMARY the full-sweep exit code and the per-file pass counts that the CI job will reproduce.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/duckdb/anofox-statistics && python3 scripts/validate_docs_sql.py && build/release/test/unittest --test-dir=test/sql</automated>
    <fails_when>Any doc file's SQL still fails the full sweep (harness exits non-zero), OR the pre-existing SQL regression suite fails — the compound command exits non-zero.</fails_when>
  </verify>
  <done>python3 scripts/validate_docs_sql.py (full 7-file sweep) exits zero and build/release/test/unittest --test-dir=test/sql still passes, proving the CI gate is green on the current tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR contributor -> CI -> DuckDB | A PR could add a hostile doc SQL block; CI runs it. The runner is a sandboxed GitHub-hosted ubuntu-24.04 with no production credentials and DuckDB has no network by default. |
| CI config -> gate | The workflow's gating correctness determines whether doc drift can bypass the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-02 | Tampering | CI gate misconfiguration lets doc drift bypass the build | high | mitigate | The harness step runs with NO continue-on-error, so a non-zero harness exit hard-fails the job; triggers cover both pull_request and push to main so drift on either path is caught. Task 1 verify asserts the trigger + step shape; Task 2 asserts the gate is actually green on the current tree. |
| T-6-01 | Elevation | doc SQL executed in CI runner | low | mitigate | GitHub-hosted runner, no production secrets in the job env, DuckDB no-network default; `permissions: contents: read` only. |
</threat_model>

<verification>
- Task 1 grep gate confirms DocsSqlValidation.yml has the build step, harness step, pull_request + push:main triggers, ubuntu-24.04 runner.
- Task 2 confirms the full sweep + SQL regression suite are green locally (the CI job reproduces this).
- Post-merge (verification phase): the DocsSqlValidation workflow runs green on GitHub Actions for the landing PR.
</verification>

<success_criteria>
- DocsSqlValidation.yml exists, is a self-contained ubuntu-24.04 build-then-validate job with no continue-on-error.
- Triggers on pull_request and push to main.
- Full harness sweep + SQL regression suite green locally, so the gate lands green.
</success_criteria>

<output>
Create `.planning/phases/06-docs-refresh-sql-validation/06-04-SUMMARY.md` when done, recording: the workflow trigger/runner/gating shape, the full-sweep exit code + per-file pass counts, and confirmation the SQL regression suite stayed green.
</output>
