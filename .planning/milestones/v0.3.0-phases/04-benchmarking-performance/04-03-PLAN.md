---
phase: 04-benchmarking-performance
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - bench/PROFILING.md
  - crates/anofox-stats-ffi/src/types.rs
  - crates/anofox-stats-ffi/src/lib.rs
autonomous: false
requirements: [PERF-03]
estimate:
  tokens: 65000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The native release build is profiled (perf + EXPLAIN ANALYZE, cargo flamegraph where useful) and the top 3 hotspots are surfaced (PERF-03)"
    - "Each top hotspot is EITHER optimized with a safe behavior-preserving change OR documented as inherent with rationale (PERF-03)"
    - "Every hotspot decision carries before/after numbers produced by the Plan-01 harness (PERF-03)"
    - "Any optimization keeps cargo test + make test green with results unchanged"
  artifacts:
    - bench/PROFILING.md
  key_links:
    - "scripts/bench.sh (Plan 01) -> before numbers -> optimize/document -> after numbers -> bench/PROFILING.md"
    - "perf record --call-graph=dwarf on build/release/duckdb -> hotspot symbols (anofox_* / DataArray::to_vec / DuckDB aggregate nodes)"
---

<objective>
Profile the native release build to surface the top hotspots, then for each of the top 3 either apply a
safe, behavior-preserving optimization OR document it as inherent — every decision backed by before/after
numbers from the Plan-01 benchmark harness (PERF-03).

Covers PERF-03 (top hotspots each optimized or explicitly documented as inherent, with before/after numbers
from the benchmark). Depends on Plan 01 (the harness IS the before/after measurement vehicle) and Plan 02
(FFI refactor should land first so profiling reflects the refactored allocation path).

Purpose: turn measurement into evidence-backed improvement — prove the extension is faster where it safely
can be, and honestly document where cost is inherent to DuckDB/algorithm design.
Output: `bench/PROFILING.md` (methodology, top-3 hotspots, per-hotspot decision + before/after numbers),
plus any safe optimizations landed in the FFI crate with the full suite still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-benchmarking-performance/04-CONTEXT.md
@.planning/phases/04-benchmarking-performance/04-RESEARCH.md
@.planning/phases/04-benchmarking-performance/04-PATTERNS.md

# Prior-plan outputs this plan builds on:
@.planning/phases/04-benchmarking-performance/04-01-SUMMARY.md
@.planning/phases/04-benchmarking-performance/04-02-SUMMARY.md

@crates/anofox-stats-ffi/src/types.rs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Install profiling prerequisites (perf, flamegraph, valgrind)</name>
  <action>Install the profiling toolchain (perf requires sudo; a Wave-0 environment prerequisite per 04-VALIDATION.md).</action>
  <instructions>
    Profiling the native release build needs `perf` on the dev box, which requires sudo (Claude cannot
    run sudo). Please run:
      sudo pacman -S perf
      cargo install flamegraph      # requires perf first
      sudo pacman -S valgrind       # for the PERF-04 leak-check noted in Plan 02
    gperftools (/usr/lib/libprofiler.so) is already installed and is the documented CPU-profiling fallback
    if perf is unavailable. DuckDB EXPLAIN ANALYZE works in the built binary with no install.
  </instructions>
  <verification>`perf --version` prints a version (and ideally `cargo flamegraph --help` and `valgrind --version` succeed), OR the gperftools fallback is confirmed usable for CPU profiling.</verification>
  <precondition>perf is installable from the Manjaro repos (`extra/perf 7.1.8-1`); sudo access required. [user_setup — environment tooling]</precondition>
  <reversibility rating="reversible">Installing dev tools; no repo changes.</reversibility>
  <resume-signal>Type "done" when perf (or the gperftools fallback) is available.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Profile the release build and surface the top 3 hotspots with before numbers</name>
  <files>bench/PROFILING.md</files>
  <read_first>
    - 04-RESEARCH.md "Profiling Stack" (perf record incantation, frame-pointers via RUSTFLAGS, gperftools fallback, EXPLAIN ANALYZE for DuckDB-side attribution) and "Hotspot Candidates" (the 3 ranked candidates: 5-array inference malloc [now refactored], DataArray::to_vec copy, DuckDB aggregate/window state serialization).
    - 04-RESEARCH.md Open Questions 1-3 (flamegraph-on-staticlib incantation, gperftools pprof availability) — resolve empirically here.
    - 04-01-SUMMARY.md — the exact bench.sh run command and results-file location (the before/after vehicle).
    - crates/anofox-stats-ffi/src/types.rs:79-89 — DataArray::to_vec (hotspot candidate #2: allocates a fresh Vec<f64> per FFI call).
  </read_first>
  <action>
    Capture BEFORE numbers first: run `bash scripts/bench.sh` (and `--full` if the machine allows) to record a
    baseline results file. Then profile the native release build (LTO/codegen-units=1/opt-level=3 is already
    configured):
    - `perf record -g --call-graph=dwarf -- ./build/release/duckdb -unsigned -cmd "LOAD '<ext path>';" -f bench/workloads/01-agg-dispatch.sql` then `perf report --no-children`; filter to `anofox_*` symbols. If unwinding is poor, rebuild once with `RUSTFLAGS="-C force-frame-pointers=yes" make release`, profile, then rebuild clean.
    - Use DuckDB `EXPLAIN ANALYZE` on the W1 aggregate query to split DuckDB-node time (HASH_GROUP_BY, partitioning) from the aggregate-function (FFI+Rust) time.
    - Optionally `cargo flamegraph`/`flamegraph -- perf script` on the duckdb binary, or the gperftools `LD_PRELOAD=/usr/lib/libprofiler.so` fallback, whichever produces usable attribution (resolve Open Questions 1 & 3 empirically; document what worked).
    Identify the top 3 hotspots by measured CPU/time attribution. Write bench/PROFILING.md with: the profiling
    methodology and exact commands used, the tool that worked, the ranked top-3 hotspots with their measured
    cost, and the captured BEFORE numbers (reference the baseline bench results file). Do not optimize yet —
    this task establishes the evidence base.
  </action>
  <verify>
    <automated>test -f bench/PROFILING.md && grep -qiE "hotspot|top 3|top three" bench/PROFILING.md && grep -qi "before" bench/PROFILING.md</automated>
  </verify>
  <reversibility rating="reversible">Adds a documentation file + captures baseline numbers; no source changes.</reversibility>
  <done>bench/PROFILING.md exists with the profiling methodology, the tool that produced usable attribution, the ranked top-3 hotspots, and captured BEFORE numbers referencing a bench.sh results file.</done>
</task>

<task type="auto">
  <name>Task 3: For each top hotspot — optimize (behavior-preserving) or document as inherent — with after numbers</name>
  <files>bench/PROFILING.md, crates/anofox-stats-ffi/src/types.rs, crates/anofox-stats-ffi/src/lib.rs</files>
  <read_first>
    - bench/PROFILING.md (from Task 2) — the ranked top-3 hotspots and before numbers.
    - 04-RESEARCH.md "Hotspot Candidates" table — the expected disposition per candidate (inference malloc: optimized via Plan 02; DataArray::to_vec: candidate for slice-not-Vec if safe; DuckDB aggregate state serialization: likely inherent, document).
    - crates/anofox-stats-ffi/src/types.rs:79-89 — DataArray::to_vec, if it surfaces as a hotspot and a safe slice-based path is available.
    - 04-CONTEXT.md "Bar for action" — optimize only where a SAFE, behavior-preserving win exists; otherwise document as inherent with rationale.
  </read_first>
  <action>
    For each of the top 3 hotspots from Task 2, apply D-CONTEXT's bar for action:
    - If a SAFE, behavior-preserving optimization exists (e.g. DataArray::to_vec allocating a fresh Vec per FFI
      call could pass a slice/borrow instead where lifetimes allow), implement it in the FFI crate. Keep changes
      minimal and localized; do NOT rewrite core numerics (out of scope per REQUIREMENTS).
    - If the hotspot is inherent (e.g. DuckDB aggregate/window state serialization is documented ~7.5 GB
      overhead for 1M groups — a DuckDB dispatch cost, not the extension's), document it as inherent with a clear
      rationale rather than forcing a risky change.
    After any optimization, re-run `bash scripts/bench.sh` to capture AFTER numbers, and run the behavior oracle
    (`cargo test` + `make test`) to confirm results are unchanged. Update bench/PROFILING.md so each of the top 3
    hotspots has: its disposition (optimized | inherent), the rationale, and before/after numbers from the harness
    (for "inherent" items, the before number plus the rationale for why no safe win exists). If an attempted
    optimization does not produce a measurable, safe improvement, revert it and document the hotspot as inherent.
  </action>
  <verify>
    <automated>cargo test -p anofox-stats-ffi && grep -qiE "optimized|inherent" bench/PROFILING.md && grep -qi "after" bench/PROFILING.md</automated>
  </verify>
  <reversibility rating="reversible">Any source optimization is behavior-preserving and small; revert if it does not produce a safe measurable win.</reversibility>
  <done>Each of the top 3 hotspots in bench/PROFILING.md has a disposition (optimized or inherent), a rationale, and before/after numbers from the bench.sh harness; any landed optimization keeps `cargo test` and `make test` green with results unchanged.</done>
</task>

</tasks>

<verification>
- bench/PROFILING.md documents the methodology, the top 3 hotspots, and for each a disposition (optimized|inherent) + rationale + before/after numbers from scripts/bench.sh.
- Any optimization landed in the FFI crate keeps `cargo test` (workspace) and `make test` (test/sql) green with results unchanged from baseline.
- The before/after numbers come from the same Plan-01 harness (apples-to-apples).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust FFI → C++ caller | any hotspot optimization (e.g. slice-instead-of-Vec in DataArray::to_vec) must not change the data the C++ side receives |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Repudiation | a "performance" optimization silently changes numeric results | high | mitigate | cargo test + make test behavior oracle must stay green with unchanged results after every optimization |
| T-04-07 | Tampering | slice/borrow optimization introduces a use-after-free or aliasing bug at the FFI boundary | high | mitigate | Only apply where lifetimes provably permit; keep the change localized; revert if not provably safe (fall back to documenting as inherent) |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new project packages; `cargo install flamegraph` is a dev-only tool (Task 1), not a project/runtime dependency |
</threat_model>

<success_criteria>
- The top hotspots surfaced by profiling are each optimized or explicitly documented as inherent, with before/after numbers from the benchmark (PERF-03).
- All optimizations are behavior-preserving; the full suite stays green with results unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-benchmarking-performance/04-03-SUMMARY.md` when done.
</output>
